/*
============================================================================
FILE : ChocolateBars.java
AUTHOR : Chrys Sean T. Sevilla
DESCRIPTION : The program should then output the number of chocolate bars that should be consumed to maintain one’s weight for both a woman and a man of the input weight, height, and age.
COPYRIGHT : 28-08-2024
REVISION HISTORY
Date: By: Description:
revision date author description of the change
revision date author description of the change
.
.
.
revision date author description of the change
============================================================================
*/

package ThirdProject;
import java.util.Scanner;

public class MultiplytheDigits {
	
	public static void main(String[] args) {
		
		//declaration of variables
		String s1;
		double weightInPounds = 0, heightInInches = 0,ageInYears = 0;
		double BMRwomen = 655 + (4.3 * weightInPounds) + (4.7 * heightInInches) - (4.7 * ageInYears);
		double BMRmen = 66 + (6.3 * weightInPounds) + (12.9 * heightInInches) - (6.8 * ageInYears);
		Scanner keyInput = new Scanner(System.in);
		
		System.out.println("Enter amount (from 0-1000 cents) :");
		s1 = keyInput.nextLine();
		originalAmount = Integer.parseInt(s1);
		
		if (originalAmount >= 0 && originalAmount <= 1000) { //input validation from 1-99 cents
			  System.out.println("It is from 0-1000 cents");
				  
			  n1 = (originalAmount/100)%10;
			  n2 = (originalAmount/10)%10;
			  n3 = (originalAmount%10);
			  System.out.println(n1); 
			  System.out.println(n2);
			  System.out.println(n3);
			  answer = n1*n2*n3;
			  System.out.println(answer); 
			}
		else {
			System.out.println("Wrong Input!"); // error message
		}
		keyInput.close();
	}
}
